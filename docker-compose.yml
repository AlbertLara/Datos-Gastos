version: "3.7"

services:
  web:
    build: ./app
    container_name: flask
    command: python main.py run -h 0.0.0.0
    volumes:
    - ./app/:/usr/src/app
    restart: always
    ports:
    - 8080:8080